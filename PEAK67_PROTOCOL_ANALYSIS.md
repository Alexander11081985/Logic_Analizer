# PEAK67 — аналіз 64 каналів

Оновлено: 2026-09-22

Джерело: 64 CSV-захоплення з
`C:\Users\Kovalenko Oleksandr\Documents\peak67`, по одному для кожного
каналу `A/B/E/F/R/P/H/U × 1…8`.

## Підтверджений інтерфейс

- CH0 / GPIO4 — CLK, idle HIGH, активний короткий LOW-імпульс.
- CH1 / GPIO5 — active-LOW CS/LE; HIGH-фронт завершує/фіксує кадр.
- CH2 / GPIO6 — DATA.
- Кадр має 32 біти, передається MSB-first.
- DATA стабільна до висхідного фронту CLK; це відповідає зчитуванню на
  висхідному фронті.
- Видима ширина коректно захоплених LOW CLK переважно 1,025–1,05 мкс.

Edge ISR не завжди читає короткий CLK до його повернення у HIGH. Тому частина
фізичних тактів у CSV має порожній `changed_channels` і не малюється як
перехід CH0. Для декодування події групуються в один такт: короткі ISR-події
0,61–1,21 мкс і повна пара CH0 LOW→HIGH належать одному кластеру. Останній
стан CH2 у кластері є переданим бітом. Завершальний CH1 rising у кластери
даних не входить.

Машинна перевірка всіх 64 файлів не дала жодної невідповідності між реально
наявними бітами та таблицею нижче. Для часткових captures перевірялися тільки
біти, які фізично є у файлі, через mask.

## A, B, E, F, R, P, H

Ці сім BAND утворюють суцільний ряд із 56 каналів. Для `i=0…55`, де A1 має
`i=0`, підтверджено:

```c
uint32_t frame = 0x05800000u + (uint32_t)i * 0x00050000u;
```

| BAND | CH1 | CH2 | CH3 | CH4 | CH5 | CH6 | CH7 | CH8 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| A | `05800000` | `05850000` | `058A0000` | `058F0000` | `05940000` | `05990000` | `059E0000` | `05A30000` |
| B | `05A80000` | `05AD0000` | `05B20000` | `05B70000` | `05BC0000` | `05C10000` | `05C60000` | `05CB0000` |
| E | `05D00000` | `05D50000` | `05DA0000` | `05DF0000` | `05E40000` | `05E90000` | `05EE0000` | `05F30000` |
| F | `05F80000` | `05FD0000` | `06020000` | `06070000` | `060C0000` | `06110000` | `06160000` | `061B0000` |
| R | `06200000` | `06250000` | `062A0000` | `062F0000` | `06340000` | `06390000` | `063E0000` | `06430000` |
| P | `06480000` | `064D0000` | `06520000` | `06570000` | `065C0000` | `06610000` | `06660000` | `066B0000` |
| H | `06700000` | `06750000` | `067A0000` | `067F0000` | `06840000` | `06890000` | `068E0000` | `06930000` |

## U BAND

U не є продовженням глобального індексу `+5`. Реальні слова:

| Канал | 32-бітний кадр |
|---|---:|
| U1 | `0x05818000` |
| U2 | `0x05A70000` |
| U3 | `0x05CF0000` |
| U4 | `0x05F70000` |
| U5 | `0x061F0000` |
| U6 | `0x06470000` |
| U7 | `0x066F0000` |
| U8 | `0x068D0000` |

U2→U7 мають крок `0x00280000`; U7→U8 — `0x001E0000`. U1 також має
ненульовий біт у третьому байті, тому його не можна представляти лише як
16-бітний код, зсунутий на 16.

Розташування U-кодів між кодами основної таблиці є сильним свідченням, що
слово пов'язане з налаштуванням частоти/дільника, а не просто з номером
BAND/CHANNEL. Перетворення коду у MHz потребує офіційної таблиці частот або
хоча б двох точно відомих пар `частота ↔ кадр`.

## Точні таймінги raw 5 MHz

Знято 12 raw-captures: по три для A1, A8, U1 та U8. Частота семплювання
5 MHz, тому квантування часу становить 0,2 мкс. Усі 12 файлів декодувалися
у правильні повні 32-бітні слова: A1=`0x05800000`, A8=`0x05A30000`,
U1=`0x05818000`, U8=`0x068D0000`.

| Параметр | Min | Avg | Max | Кількість |
|---|---:|---:|---:|---:|
| CLK LOW | 0,8 мкс | 1,027 мкс | 1,2 мкс | 372 |
| CLK HIGH | 5,0 мкс | 5,038 мкс | 5,2 мкс* | 360 нормальних |
| CLK rising→rising | 6,0 мкс | ≈6,065 мкс | 6,4 мкс* | 360 нормальних |
| DATA після CLK falling | 0,6 мкс | 0,764 мкс | 0,8 мкс | 78 змін DATA |
| DATA setup до CLK rising | 0,2 мкс | 0,328 мкс | 0,6 мкс | 78 змін DATA |
| DATA hold після CLK rising | 5,6 мкс | 5,792 мкс | 5,8 мкс | 77 змін DATA |
| останній CLK rising→CS rising | 5,0 мкс | 5,05 мкс | 5,2 мкс | 12 |

`*` В `TIMING_A8_2` один раз перед 9-м зчитаним бітом HIGH був подовжений
до 10,6 мкс, а rising→rising period — до 11,6 мкс. Кадр попри це лишився
правильним `0x05A30000`. Це одинична пауза, а не системна байтова межа:
в інших 11 captures її немає. Причину цієї одиничної затримки з CSV
встановити неможливо.

Фізична фаза: CLK idle HIGH; перед кожним бітом CLK падає, приблизно через
0,6–0,8 мкс контролер за потреби змінює DATA, ще через 0,2–0,6 мкс CLK
піднімається і приймач зчитує біт. Далі CLK лишається HIGH приблизно 5 мкс.
Отже номінальна швидкість становить близько 165 кбіт/с. Перший інтервал
CS falling→CLK не виміряний, бо CS falling є тригером і pre-trigger у цих
raw-captures відсутній.

## BOOT-захоплення

Перевірено три edge-captures тривалістю приблизно 20 мс. У жодному немає
нормальної 32-тактової команди. Найдовші послідовності CLK rising з періодом
4,5…7,5 мкс містять лише 3, 3 та 4 фронти відповідно. Переходи нерегулярні,
з одночасною активністю кількох ліній. За формою це не валідні
PEAK67-кадри; фізичну причину цих переходів ці captures не визначають.

Файл `BOOT_U8 → 0x068D0000.csv` за уточненням користувача фізично знятий на
каналі U1. Його ім'я і hex-мітка помилкові; `0x068D0000` із цього capture не
декодується. Так само hex у назвах інших BOOT-файлів є лише мітками, а не
результатом декодування.

Поточний доказ обмежений першими 20 мс: він показує, що в цьому вікні
звичайний 32-бітний кадр не передавався. Для перевірки пізнішої ініціалізації
потрібен 100-мс edge-capture від окремого безпечного 3,3-вольтового сигналу
живлення/enable на CH3 або інший pre-trigger від моменту подачі живлення.

### Capture 603 та повторний `start_A8`

`logic_capture_603.csv` спочатку був помилково позначений користувачем як
запуск A2. Канал першого запуску фактично не був зафіксований; після повторної
перевірки найбільш імовірно, що це також був A8. Окремий `start_A8.csv` точно
знятий під час запуску A8. В обох файлах trigger CH1 falling дочекався
початку активної передачі.

Capture 603 містить сім окремих CS-low кадрів:

| № | CS LOW, мкс | Пауза до наступного CS LOW | Edge-cluster decode |
|---:|---:|---:|---:|
| 1 | 195,263 | 1,025 мкс | `0x00580005` |
| 2 | 195,725 | 2,263 мкс | `0x0080802C` |
| 3 | 194,913 | 4,588 мкс | `0x000004B3` |
| 4 | 193,863 | 2,263 мкс | `0x00068F42` |
| 5 | 194,538 | 4,588 мкс | `0x08008011` |
| 6 | 191,825 | 15,038 мкс | `0x05800000` (відоме A1) |
| 7 | 195,475 | — | `0x05A30000` (відоме A8) |

Серія від першого CS falling до завершення сьомого кадру займає
1391,363 мкс. Незалежний `start_A8.csv` дає біт у біт ту саму послідовність:

`00580005 0080802C 000004B3 00068F42 08008011 05800000 05A30000`

Отже попередній висновок про «запуск A2 без слова `0x05850000`» скасований:
він спирався на неправильну мітку каналу. Два однакові A8-запуски натомість
відтворювано показують п'ять однакових стартових слів, потім відоме A1-слово
і останнім відоме A8-слово. Щоб перевірити, чи останній кадр справді завжди
задає збережений канал, потрібен правильно позначений запуск A2 або U1.

У `start_A8.csv` короткий CS HIGH→LOW між кадрами 2 і 3 не потрапив до edge
events, тому на рівні CS вони виглядають як один LOW-інтервал. Усередині нього
є рівно 64 clock cells, які без залишку декодуються як ті самі кадри 2 і 3.
Це втрата короткої CS-події edge ISR, а не один 64-бітний протокольний кадр.

Час `t=0` тут є моментом першого CS falling, а не моментом подачі живлення;
затримка від power-on до цієї серії не виміряна. Отже результат не суперечить
попереднім 20-мс captures, які були прив'язані до раннього фронту CLK.

Перші п'ять слів в обох captures відновлені через вже перевірене групування
edge ISR events у 32 clock cells. Кадри 6 і 7 точно потрапили у відомі
A1/A8-значення, що є незалежною перевіркою вирівнювання. Остаточна побітова
перевірка всієї серії:
raw 5 MHz, 8192 samples, trigger CH1 falling; вікно 1,6384 мс охоплює всі
сім кадрів.

### `A2_start_CH1_triger.csv`: перевірка виправленого raw trigger

Після прошивки ANYEDGE-виправлення новий raw capture на CH1 falling дає всі
сім логічних 32-бітних слів:

`00580005 0080802C 000004B3 00068F42 08008011 05800000 05850000`

Отже дефект trigger виправлено і це підтверджено на реальному сигналі. Перший
фізичний CLK rising відбувся безпосередньо перед `t=0` через ISR latency, але
початковий семпл зберіг стан CLK=HIGH та DATA першого біта. Тому всі 32 логічні
біти першого слова відновлюються однозначно; лише абсолютний час від CS falling
до першого CLK rising цим файлом не вимірюється.

Для кадрів 2–7: 32 rising edges на кадр, CLK period переважно `6,0…6,4 мкс`,
CLK LOW `0,8…1,2 мкс`, останній CLK rising → CS rising `5,0…5,2 мкс`.
Послідовність закінчується A1=`0x05800000`, потім збереженим
A2=`0x05850000`. Разом із двома A8 captures це остаточно підтверджує стартову
схему: п'ять постійних init-слів → A1 → збережений канал.

### `A4_start_CH1_triger.csv`: незалежне підтвердження на A4

Новий raw 5 MHz capture містить сім 32-бітних логічних кадрів:

`00580005 0080802C 000004B3 00068F42 08008011 05800000 058F0000`

Перші шість кадрів біт у біт збігаються з A2 та A8. Лише сьомий кадр
змінюється на очікуване A4=`0x058F0000`, що відповідає перевіреній формулі
каналів A: `0x05800000 + channel_index * 0x00050000`.

Записано 224 логічні біти. У першому кадрі перший фізичний rising передує
`t=0` і біт береться з початкового стану; у кадрах 2–7 фізично присутні всі
32 rising edges. Внутрішньокадровий CLK period min/avg/max =
`6,000/6,067/6,400 мкс`, CLK LOW = `0,800/1,028/1,200 мкс`; останній CLK
rising → CS rising у кожному кадрі дорівнює `5,0 мкс`.

Три різні збережені канали A2, A4 та A8 тепер незалежно підтверджують один
алгоритм старту: п'ять постійних init-команд → A1 → збережений канал.

### `start_A2.csv`: перший capture до виправлення trigger

Правильно позначений запуск A2 записано в raw 5 MHz. Початок capture припав
приблизно на п'ятий біт першого кадру, тому від нього є лише 28-бітний хвіст.
Цей хвіст точно збігається з молодшими 28 бітами `0x00580005`. Наступні шість
кадрів повні:

`0080802C 000004B3 00068F42 08008011 05800000 05850000`

Отже порівняння запусків A8 та A2 підтверджує структуру без припущень:

1. перші п'ять 32-бітних слів постійні;
2. шостим передається A1=`0x05800000`;
3. сьомим передається збережений канал: A8=`0x05A30000` або
   A2=`0x05850000`.

Обрізаний початок A2 також виявив firmware-дефект raw trigger. При arm із
неживленим приймачем CH1 уже LOW. Raw-режим дозволяв тільки falling interrupt,
тому не бачив проміжний rising, залишав remembered state LOW і відкидав
наступний реальний falling як хибне LOW→LOW. Виправлення: на trigger GPIO у
raw-режимі дозволити ANYEDGE для синхронізації стану, а `trigger_matches()`
як і раніше запускає capture тільки на вибраному фронті.

## Повні та часткові captures

Повні кадри без пропущеного префікса отримані для 46 каналів. У папці
частковий початок мають:

`B1, B2, B4, B5, B6, B7, B8, E1, F6, F7, R7, R8, P1, P6, H6, H7, U1, U4`.

Їхні наявні DATA-хвости без помилок збігаються з таблицею. U1 додатково був
підтверджений окремим повним захопленням `0x05818000`. Решта пропущених
префіксів відновлюється однозначно з сусіднього ряду та перевіреної mask.

Для еталонної осцилограми всіх фізичних CLK рекомендовано raw capture:
5 MHz, 2048 samples, trigger CH1 falling.
